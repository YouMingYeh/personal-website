import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#4d6160",
    },
    secondary: {
      main: "#292522",
    },
    background: {
      default: '#a39081',
    },
  },
});

export default theme;
